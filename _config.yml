title: "define TRUE FALSE"
description: "#define TRUE FALSE"
author:
  name: "#define"
  email: "define@define.sh"
  url: "https://define.sh"

baseurl: /

source: .
destination: ./_site
permalink: /:title
paginate: 3
paginate_path: /page/:num/

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

future: true
plugins:
  - jekyll-seo-tag
  - jekyll-paginate
  - jekyll-sitemap

# Custom variables
owner : "#define"
year : "2022"
